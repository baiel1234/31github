class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        result = ''.join(map(str, digits))
        s = int(result) + 1
        integer_list = [int(digit) for digit in str(s)]
        return integer_list
